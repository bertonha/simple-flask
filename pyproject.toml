[project]
name = "simple-flask"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
license = {file = "LICENSE"}
dependencies = [
    "flask>=3.1.0",
    "gunicorn == 23.0.0",
]

[tool.uv]
dev-dependencies = [
    "mypy>=1.13.0",
    "pytest>=8.3.3",
    "ruff>=0.7.3",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = "."
testpaths = ["tests"]
